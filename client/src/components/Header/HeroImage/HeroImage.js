import React from "react";

const HeroImage = props => (
    <div className="jumbotron">
        <h1 className="display-4">Create your own 'To-Read' list</h1>
        <p className="lead">Search for any books by title or author - save them for later.</p>
        <hr className="my-4" />
        <p>Results from Google Books.</p>
    </div>

)

export default HeroImage;